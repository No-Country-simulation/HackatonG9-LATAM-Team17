# ---------------------------------------------------------------------
# BASE DE DATOS (Ajustar según tu entorno local / PostgreSQL / MySQL)
# ---------------------------------------------------------------------
DB_URL=jdbc:postgresql://localhost:5432/${DB_NAME} // tu_base_de_datos
DB_USERNAME=${DB_USERNAME} // tu_usuario
DB_PASSWORD=${DB_PASSWORD} // tu_contraseña